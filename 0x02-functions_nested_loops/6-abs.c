#include "main.h"

/**
 * _abs - computes absolute value of an integer
 *
 * Return: absolute value
 */
int _abs(int)
{
	int i;
	if (i >= 0)
		return (i);
	else
		return (i * -1);

	_putchar('\n');
}
